# 2026-08-26 — Runner Build Repair Targets the Recorded E2E Blocker

## Runner Recovery
- Pull request [#133](https://github.com/clawosiris/rust-gvm-e2e-tests/pull/133) updates the locked `rust-gvm` revision and migrates target creation to the current validated host/port API.
- The repair removes a broad non-fatal cleanup path that masked runner-build failures and adds explicit checks that both required runner binaries exist before the image build succeeds.

## Regression Coverage
- The pull request adds request-serialization coverage for the migrated target builder and reports passing format, workspace check, test, audit, and direct `gvm-community-e2e` installation validation.

## Action Required
- Pull request #133 is open and mergeable, but GitHub currently reports no completed PR checks. Its runner-image and live-stack workflow needs to validate the fix before merge because local Docker/Compose validation was unavailable.
