# 2026-08-12 — Mainline Dispatches Exposed a Missing Runner Binary

## E2E Regression
- Eight workflow-dispatch runs against unchanged `main` commit `aae5c9ed` failed while validating successive `rust-gvm` mainline merges.
- The Community E2E job reaches a responsive gvmd 26.36.1 instance, then fails when Docker tries to launch the test runner because `gvm-community-e2e` is not present in the runner image `PATH`.
- The same failure propagates back to the triggering `rust-gvm` main CI workflows, making this a new cross-repository integration blocker rather than an unchanged scheduled-run signal.

## Pull Request State
- Dependabot refreshed [#120](https://github.com/clawosiris/rust-gvm-e2e-tests/pull/120) again for `actions/checkout` 7.0.1. Its earlier successful E2E validation remains the latest PR result; the newly observed failure is on externally dispatched mainline validation.
