# 2026-07-06 — Mainline Finally Picked Up Workflow and Dependency Maintenance, but the Harness Still Splits Clean Manual Runs from Failing `repository_dispatch` Paths

## Mainline & Merged Work

### The published journal baseline on `origin/main` was still 2026-05-07, and the repo did move after that point
- `main` absorbed three non-journal merges in the unpublished window: PR #63 added journal PR automation on 2026-05-18, PR #61 bumped `quick-xml` to 0.40 on 2026-05-25, and PR #70 refreshed the GitHub Actions dependency group later that same day.
- That is still maintenance work rather than new test-scenario engineering, but it matters because the repo was not actually frozen after the last published entry. The harness and its automation surface both changed on `main`.

## Issues
- One new non-journal issue appeared in this window: #56 proposed adding openvasd HTTP scanner API E2E lanes alongside the legacy ospd-openvas coverage, then closed the same day on 2026-05-13.
- That issue did not turn into a visible long-lived backlog item, so the issue story is still narrower than a normal feature push. The repo briefly revisited test-surface expansion, then went back to maintenance and execution posture.

## Pull Request Queue
- The visible queue is still dominated by journal backlog, but the active non-journal lane has changed. The old broken actions bump PR #82 was closed on 2026-07-06, and fresh Dependabot follow-ups opened immediately as PRs #108 and #109.
- That reset is more meaningful than the raw queue size because it replaces a stale dependency branch with current maintenance work that already validated on the automation side.

## CI Health
- Manual `workflow_dispatch` runs kept the harness usable throughout the unpublished window. Successful `E2E Tests` runs were recorded on 2026-06-18, 2026-07-01, 2026-07-02, and 2026-07-03.
- The dispatch-path split is still the actionable problem. `repository_dispatch` failed repeatedly on 2026-06-18 and failed again on 2026-07-05 even while the manual dispatch path stayed green.
- That leaves the repo in a clearer but still unresolved posture: dependency freshness and automation upkeep resumed on `main`, the harness still works when triggered manually, and the remaining operational gap is the external dispatch path rather than the core test flow itself.
