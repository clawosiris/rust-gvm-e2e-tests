# 2026-07-16 — Mainline Took the Version Parameterization and Dependency Refreshes

## Mainline Delivery
- Since the published 2026-07-09 journal entry, `main` advanced for the first time in weeks through three non-journal merges.
- Dependabot PR [#108](https://github.com/clawosiris/rust-gvm-e2e-tests/pull/108) updated the test-library `quick-xml` requirement from `0.40` to `0.41`, and PR [#109](https://github.com/clawosiris/rust-gvm-e2e-tests/pull/109) refreshed the GitHub Actions group.
- PR [#115](https://github.com/clawosiris/rust-gvm-e2e-tests/pull/115) then landed the requested `gvm-version` workflow input and Compose `GVM_VERSION` parameterization, while preserving the default `stable` stack behavior.

## Backlog
- Issue [#16](https://github.com/clawosiris/rust-gvm-e2e-tests/issues/16) is now closed by the version-parameterization merge, so the harness can be driven against non-default GVM stack tags without editing workflow or Compose source.
- Cross-repo issue [#113](https://github.com/clawosiris/rust-gvm-e2e-tests/issues/113) is also closed after the API-side branch-specific E2E gate migration landed, leaving the repo's immediate non-journal backlog smaller than it was before the consolidation pass.

## Validation
- The merged PRs all carried green `E2E Tests` coverage on their pull-request heads.
- There is still no fresh post-merge `main` E2E run after commit `010e6a6`, so the old 2026-07-09 repository-dispatch CLI break should not be treated as resolved by these merges.
