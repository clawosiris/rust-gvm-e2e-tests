# 2026-06-20 — Mainline Finally Moved Past Pure Journal Churn, but the Repo Still Looks Like an Ops Queue with Split Trigger Health

## Repository Activity

### The repo did absorb real maintenance work after the 2026-05-07 baseline, even though journal backlog still dominates the branch list
- `main` picked up PR #45 on 2026-05-07 to move `quick-xml` from 0.38 to 0.39, then later absorbed PR #63 (journal PR automation workflow), PR #61 (`quick-xml` 0.40 in `/tests/library`), and PR #70 (actions-group dependency bumps).
- That is a meaningful change from the previous journal entry, which described a repo stuck on journal-only movement. Mainline did advance in dependency and workflow maintenance, even if no new test-lane features shipped.
- The repo's visible work is still operational rather than product-led: most motion after that came from automation, dependency freshness, and repeated journal PR accumulation rather than new test coverage areas.

### One feature-shaped issue appeared, then disappeared without turning into an active branch queue
- Issue #56 opened on 2026-05-13 to add openvasd HTTP scanner API E2E lanes alongside legacy ospd-openvas validation, then closed the same day.
- That leaves the issue tracker nearly empty again, which keeps the repo's story focused on maintenance and execution behavior rather than backlog expansion.

## Issues
- The repo saw one issue opened and one issue closed in this window, both for #56.
- Because that ticket did not turn into a visible long-lived branch train, the issue tracker is still not where the repo's friction shows up. The real friction remains in queue management and workflow behavior.

## Pull Request Queue
- The open queue is overwhelmingly journal backlog. Daily journal PRs now stretch through #94, while the only visible non-journal open branch is dependency PR #82.
- That is even more backlog-shaped than the 2026-05-07 entry described: some older journal PRs were merged, but the automation stream continued to outpace review and merge throughput.

## CI Health
- The latest visible signal is sharply split by trigger type rather than by branch. Manual `workflow_dispatch` runs of `E2E Tests` on 2026-06-18 succeeded multiple times on `main`.
- In contrast, `repository_dispatch` executions on the same day failed repeatedly, so the repo still has an orchestration-path problem even when the underlying test job can pass.
- The result is a usable but uneven test surface: operators can still get green manual runs, but automation-driven entry points remain the unstable lane.
