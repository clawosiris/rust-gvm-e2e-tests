# 2026-06-19 — Mainline Stayed in Test-Harness Maintenance Mode While the Live E2E Signal Split Between Clean Manual Runs and Failing Dispatch Traffic

## Repository Activity

### Mainline only moved for harness maintenance, not for a new feature wave
- Since the 2026-05-07 baseline, `main` absorbed just three merges: PR #63 added the journal PR automation workflow, PR #61 bumped `quick-xml` in the test library, and PR #70 updated the GitHub Actions stack
- That sequence matters because it confirms the repo did not reopen broad E2E-surface development after early May. The only shipped work in this window was automation and dependency maintenance, not a new round of scanner coverage or test-harness expansion
- The repository is therefore quieter than the queue shape suggests: mainline changed, but almost all of that change was operational housekeeping

### The open queue is now almost entirely journal backlog plus one small maintenance branch
- Open PRs now span journal entries #62 through #93, with PR #82 as the only non-journal branch still visible in the queue
- That is a backlog-shaped queue rather than an implementation queue. The repo has accumulated documentation automation output much faster than it has accumulated new substantive test-harness work

## Issues
- Only one issue opened after the 2026-05-07 baseline: #56, which tracked adding openvasd HTTP scanner API E2E lanes alongside the legacy ospd-openvas validation path
- That same issue also closed on 2026-05-13, so there was brief issue churn without a corresponding long-lived backlog tail
- Aside from that one spike, the issue tracker stayed mostly static and the older open items remain the same small pair: #16 (multi-version stack parameterization) and #7 (persistent-runner cleanup)

## CI Health
- The current execution signal is mixed but interpretable rather than random. On 2026-06-18, repeated manual `workflow_dispatch` E2E runs on `main` succeeded against the latest head SHA
- In the same window, multiple `repository_dispatch` E2E runs against that exact same commit failed, often interleaved with successful manual runs
- That split matters more than the raw failure count. The repo does not look generally broken on `main`; it looks like externally triggered integration traffic is less stable than locally initiated/manual validation on the same test baseline
