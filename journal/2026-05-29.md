# 2026-05-29 — `main` Finally Moved Again, but Only in Maintenance and Automation Lanes While Validation Stayed Mostly Operational

## Repository Activity

### Mainline changed after the 2026-05-07 baseline, but the movement was still support work rather than new test-surface delivery
- PR #63 (`ci: add journal PR automation workflow`) merged on 2026-05-18, giving the repo the same journal automation lane now visible across the broader GVM set
- PR #61 updated `quick-xml` in `/tests/library`, and PR #70 bumped the actions group on 2026-05-25, so the only real `main` code movement in this window was dependency and workflow maintenance
- Issue #56 briefly opened and closed on 2026-05-13 to add openvasd HTTP scanner API E2E lanes alongside the legacy ospd-openvas path, but that work did not produce a visible `main` merge in this repository during the same window
- In other words, the repo did not stay frozen, but it still behaved more like an execution harness being maintained than a product surface expanding

## Pull Request Queue
- Journal backlog dominates the visible queue again: PRs #64 through #73 are all open journal entries, including the still-open 2026-05-28 branch
- The oldest notable holdover is PR #62 (`docs(journal): add 2026-05-18 journal entry`), which is now blocked rather than clean
- Unlike the 2026-05-07 entry, the open queue no longer centers on a clearly broken dependency PR; it is now mostly reporting backlog plus journal maintenance churn

## Issues
- Issue #56 is the only issue churn in the window: it opened and closed on 2026-05-13, indicating quick scope definition around the HTTP scanner API E2E lane but no sustained issue backlog growth after that
- No other issues opened or closed after the 2026-05-07 baseline

## CI Health
- Manual `E2E Tests` runs on `main` remained usable through the window, with successful `workflow_dispatch` executions on 2026-05-18, 2026-05-25, and 2026-05-28
- The one meaningful regression signal is a failed `repository_dispatch`-triggered `E2E Tests` run on 2026-05-25, which matters because cross-repo dispatch is exactly how upstream changes are supposed to fan into this harness
- PR #70 validated cleanly, and the recent failures were confined to one dynamic Dependabot update lane plus the single dispatch-triggered E2E run, so the repo is operational but still more fragile in cross-repo triggering than in manual execution
