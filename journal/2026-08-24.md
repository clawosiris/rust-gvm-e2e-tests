# 2026-08-24 — Actions Refresh Exposes the Existing Runner Blocker

## Dependency Automation
- Dependabot closed superseded pull request [#120](https://github.com/clawosiris/rust-gvm-e2e-tests/pull/120) and opened replacement [#131](https://github.com/clawosiris/rust-gvm-e2e-tests/pull/131).
- The replacement updates `actions/checkout` from 7.0.0 to 7.0.1 and `docker/setup-buildx-action` from 4.2.0 to 4.3.0.

## Validation State
- The runner image builds successfully, but the Community E2E job fails on pull request #131.
- This is not a new failure mode: the unchanged `main` runner also continued failing dispatched validations after the previous journal entry because `gvm-community-e2e` is absent from the runner image `PATH`.
- Pull request #131 therefore remains blocked by the already-recorded cross-repository runner-image defect.
