# 2026-09-25 — E2E Harness Tightens Readiness and Stop-Task Semantics

## Mainline & Release Work

### Five non-journal PRs merged on `main`
- PR #149 qualified the canonical rust-gvm revision used by the E2E suite, closing the related mainline migration and provenance work.
- PR #151 isolated E2E runner invocations in CI, preventing one runner execution from inheriting another's dependency context.
- PR #154 now requires the exact asynchronous `stop_task` success status (`202`), tightening the protocol contract exercised by the suite.
- PRs #136 and #140 updated Quick-XML and GitHub Actions dependencies; the corresponding lockfile and workflow changes are now on `main`.

### Feed aggregation remains an active reliability focus
- New PR #155, `fix(e2e): wait for feed aggregation readiness`, extends the authenticated readiness gate to require inactive feed synchronization plus successful CVE, CPE, and CERT probes before expensive tests run.
- The PR preserves the existing hard deadline and reconnect behavior rather than weakening assertions or timeouts; it is pending independent review and merge.

## Development Queue
- The newly opened readiness work in PR #155 is the current attention item: after it merges, Issue #147 qualification should restart against the resulting exact `main` SHA.

## Issues
- Issues #143–#146 were closed with the canonical-request migration, session-safe readiness polling, immutable rust-gvm revision selection, and provenance documentation delivered on `main`.
- Issue #147 remains the follow-on qualification tracker for validating the repaired harness on warm and clean stacks.

## CI Health
- Mainline CI received a concrete runner-isolation fix in PR #151.
- No new actionable CI or security regression was observed; the remaining readiness change is tracked explicitly in open PR #155.
