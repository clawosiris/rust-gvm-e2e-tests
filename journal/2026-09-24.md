# rust-gvm-e2e-tests - 2026-09-24

## Summary of Activity
The canonical rust-gvm qualification change merged to `main`, closing its four supporting E2E issues. A focused follow-up PR is now open to repair the first warm qualification failure without changing the test contract.

## Detailed Changes

### Merged Qualification Work
- PR #149: `fix(e2e): qualify canonical rust-gvm revision` merged into `main`.
  - Moved the live harness to canonical requests, typed responses, normalized server errors, and one protected rust-gvm revision.
  - Strengthened persistent-runner recovery with bounded authenticated reconnects, readiness handling for feed databases, and safe PostgreSQL checkpoint/teardown safeguards.
  - Closed issues #143–#146 covering canonical v0.7 request migration, session-safe readiness polling, an immutable rust-gvm revision, and provenance documentation.

### Active Follow-up
- PR #151: `fix(ci): isolate E2E runner invocations` opened after the first warm-targeted qualification run exposed a containerd `AlreadyExists` error when ephemeral Compose runners retraversed the already-started GVM dependency graph.
  - Adds `--no-deps` to runner invocations and documents the stack-ownership invariant.
  - Its successful PR workflow validates the focused correction; the remaining warm-targeted, warm-full, authorized-clean, and cross-repository evidence sequence remains tracked by issue #147.

## Overall Assessment
The canonical E2E qualification surface is now on `main`; the remaining blocker is isolated to workflow orchestration rather than the protocol assertions or persistent-stack recovery contract.
