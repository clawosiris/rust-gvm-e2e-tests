# 2026-07-09 — Repository Dispatch Surfaced a New gvm-rools CLI Break

## CI Health
- `main` itself stayed code-static after the published 2026-07-07 journal entry, but default-branch validation changed in a way that matters: the 2026-07-08 23:47 UTC `repository_dispatch` run of `E2E Tests` failed again on `main`
- This is not the previously documented clean-feed import timeout. Layer 1 GMP smoke coverage passed, and the follow-on `gvm-tools` validation also reported all checks succeeded before the run broke in Layer 2 CLI coverage
- The new failure point is explicit and narrower: `/workspace/tests/cli/smoke.sh: line 60: gvm-cli: command not found` while exercising `gvm-rools` ref `8bff56d5bf98ed92820c9b1766d2a2992d8cf2aa`, the freshly merged `main` commit from `gvm-rools` PR #144

## Posture Change
- That moves the actionable problem away from generic “dispatch path still fails” noise and toward a more specific downstream compatibility break between this harness and the current `gvm-rools` CLI surface or packaging layout
- The repo still has no new local commits, merged PRs, or issue movement after the 2026-07-07 cutoff, and the last manual `workflow_dispatch` run on `main` remains the 2026-07-06 green result
- The meaningful update for today is therefore operational rather than code-delivery: `repository_dispatch` now fails for a newly visible reason that a maintainer can actually chase
