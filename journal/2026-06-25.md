# 2026-06-25 — Mainline Finally Took the Waiting Maintenance Merges, but the Queue Is Still Mostly Journal Backlog and `repository_dispatch` Remains the Noisy Lane

## Repository Activity

### `main` did eventually move after the 2026-05-07 baseline, but only in operational lanes
- The first meaningful post-baseline merge was PR #63 on 2026-05-18, which added journal PR automation.
- That was followed on 2026-05-25 by two maintenance merges: PR #61 updated `quick-xml` in `/tests/library` from `0.39` to `0.40`, and PR #70 bumped the shared GitHub Actions group across the repo.
- That sequence matters because the repo finally converted the old open maintenance queue into `main` changes, but it still did not ship new E2E coverage or major harness behavior. The visible movement stayed operational rather than product-expanding.

## Issues
- The only issue churn in this window was tightly scoped and fast. Issue #56 opened and closed on 2026-05-13 to track openvasd HTTP scanner API E2E lanes alongside the legacy `ospd-openvas` path.
- No other issues opened or closed after that, so the tracker still looks sparse rather than actively managed.
- That means the repo's real pressure is not bug backlog. It is review throughput and the reliability of specific execution paths.

## Pull Request Queue
- The open queue is still overwhelmingly backlog-shaped. Journal PRs now span #62 through #98 with only intermittent merges, which keeps the visible branch list crowded even though the actual product change rate is low.
- The only clearly non-journal open branch in view is PR #82, another GitHub Actions bump that has been sitting since 2026-06-08.
- Compared with the 2026-05-07 entry, the queue is not cleaner yet. Some maintenance work finally landed on `main`, but the repo still presents itself as an automation backlog more than a deliberately staged test-program roadmap.

## CI Health
- Manual `workflow_dispatch` execution remained the stable lane throughout June. Successful `E2E Tests` runs continued across 2026-06-01, 2026-06-02, 2026-06-03, 2026-06-08 through 2026-06-18, and several days recorded multiple green manual runs.
- The noisy path is still `repository_dispatch`. On 2026-06-18, repeated `repository_dispatch`-triggered `E2E Tests` runs failed on `main` even while same-day manual dispatches succeeded.
- `Dependabot Updates` stayed mixed rather than clean, with successes and failures interleaved on 2026-06-01, 2026-06-08, 2026-06-15, and 2026-06-22. So the repo has usable validation signal, but only if maintainers trust the manual lane more than the automated dispatch path.
