# 2026-09-17 — Runner Build Becomes Fail-Closed, Exposing API Drift

## E2E Runner Repair
- Pull request [#138](https://github.com/clawosiris/rust-gvm-e2e-tests/pull/138) merged Rust 1.89 runner builds, removed the broad failure mask around `cargo install`, required both runner executables before image export, and migrated target construction to rust-gvm's validated typed values.
- The change converts the previous missing-executable failure into an earlier, actionable image-build failure instead of exporting an incomplete runner.

## New Integration Blocker
- Subsequent dispatches from `greenbone-hive/rust-gvm` fail while compiling `gvm-community-e2e` against current rust-gvm `main`: the target free builders, options, and error imports removed by the canonical-request migration are still referenced by the E2E client.

## Action Required
- Migrate the E2E target lifecycle to the new canonical request structs, refresh the rust-gvm lock revision, and rerun the downstream workflow to restore the external gate.
