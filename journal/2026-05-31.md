# 2026-05-31 — Mainline Finally Picked Up the Maintenance Queue, but the Journal Backlog Is Still the Dominant Open Branch Story

## Repository Activity

### Mainline moved after the 2026-05-07 entry, but it still moved more through maintenance and automation than through new test-surface expansion
- `main` absorbed PR #61 (quick-xml 0.40), PR #70 (actions-group updates), and PR #63 (journal PR automation workflow), so the repository did at least convert some long-lived maintenance work into landed state instead of leaving everything in queue
- The only new issue activity in the window was #56 (`feat: add openvasd HTTP scanner API E2E lanes alongside legacy ospd-openvas validation`), which opened and closed on 2026-05-13 without a corresponding feature merge to `main`
- That means the repo is still behaving like an operational support repository: dependency refreshes and automation are landing, but the bigger expansion of the E2E surface is still not what reached the default branch

### The open queue is overwhelmingly journal backlog rather than product work
- PRs #62, #64, #65, #66, #67, #68, #69, #71, #72, #73, and #74 are all journal branches that never made it back to `main`
- There is no comparable stack of open feature branches behind them, so the repo's review latency remains more visible than its delivery throughput

## Issues
- Issue #56 opened and closed on 2026-05-13, which at least records demand for openvasd HTTP scanner API coverage even though no corresponding implementation landed in this repository during the same window
- No other issue churn showed up after the 2026-05-07 entry, so the repository still looks quiet on explicit backlog management and heavy on operational drift

## CI Health
- Manual `E2E Tests` runs on `main` were usable throughout the window, with successful `workflow_dispatch` executions on 2026-05-18, 2026-05-25, and 2026-05-28
- The main noisy signal is still automation around maintenance branches: a `repository_dispatch` `E2E Tests` run failed on 2026-05-25, and the `Dependabot Updates` lane on the same day split between success and failure
- So the repo is not broadly broken, but it also is not yet clean enough to treat every automation path as equally trustworthy
