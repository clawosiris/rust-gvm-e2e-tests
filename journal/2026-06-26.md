# 2026-06-26 — Mainline Finally Took Real Maintenance Work, but the Queue Is Now Almost Entirely Journal Backlog While E2E Signals Stay Split by Trigger Type

## Repository Activity

### `main` did more than absorb journal backlog this time, but the actual code movement was still narrow and operational
- After the 2026-05-07 baseline, `main` absorbed PR #63 (add journal PR automation workflow) on 2026-05-18, then PR #61 (`quick-xml` 0.39 to 0.40) and PR #70 (actions-group bumps) on 2026-05-25
- That sequence matters because it finally broke the pattern the previous entry described. The repo did take genuine maintenance work on `main`, but it still did not reopen into broader test-surface expansion or CI-repair engineering
- The visible product-facing idea in this window was issue #56 for adding `openvasd` HTTP scanner API E2E lanes alongside legacy `ospd-openvas` coverage, but it was opened and closed on the same day without any merged implementation on `main`

### The queue shape got much noisier even though `main` itself stayed comparatively quiet
- The open PR list is now dominated by journal automation. PRs #75 through #99 cover daily entries from 2026-05-31 through 2026-06-25 and make up the overwhelming majority of the visible queue
- There is one newer non-journal maintenance branch in that span: PR #82 bumps `actions/checkout` from 6.0.2 to 6.0.3
- That is a worse queue shape than the 2026-05-07 entry described because the backlog problem is no longer a handful of noisy branches. It is now an almost continuous wall of unmerged journal PRs

## Issues
- One issue opened after the 2026-05-07 baseline: #56, which proposed `openvasd` HTTP scanner API E2E coverage alongside legacy scanner validation
- That issue also closed the same day, so the tracker did not grow into a larger active backlog
- The issue story is therefore still operational rather than product-led. The repo hinted at future scope expansion, but it did not convert that scope into merged `main` work

## CI Health
- The E2E signal is still split by trigger type instead of simply green or red. Manual `workflow_dispatch` runs on `main` were repeatedly successful through mid-June, including a dense cluster of green runs on 2026-06-18
- `repository_dispatch` runs on that same day were a different story: multiple back-to-back executions failed even while manual dispatches succeeded
- Dependabot automation looks cleaner than it did in early May. The latest visible `Dependabot Updates` runs on 2026-06-22 all succeeded, so the current instability is more about dispatch-path behavior than broad branch health
