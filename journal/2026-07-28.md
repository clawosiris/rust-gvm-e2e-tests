# 2026-07-28 — Community Coverage Expanded Into a Compile-Enforced Matrix

## Coverage Expansion
- PR [#122](https://github.com/clawosiris/rust-gvm-e2e-tests/pull/122) opened a broad completion pass for `rust-gvm/devel`, adding a compile-enforced Community coverage manifest and generated documentation across discovery, reversible resource lifecycles, scan/report linkage, isolated administration, transport, and differential lanes.
- The work also makes cleanup run-scoped and interruption-resilient, and fixes live-contract handling for resource-name requests and gvmd's provisional report ID `0`.
- Iterative real-stack runs have moved the pull-request coverage check from early failures to success; the latest runner-image build is still in progress, and the report-format lane remains pinned to the unmerged upstream validator fix in `rust-gvm#410`.

## New Upstream Finding
- The expanded sync lane exposed `rust-gvm#414`: the typed client sends an ID-bearing `sync_config` request even though current gvmd accepts only the schema-defined global parameterless command.
